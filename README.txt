The program was ran in Eclipse using JDBC.
The database has user root password Password1 and a localhost.
Both programs need the correct url, user, and password values coded into them.
The variables are at the top of both java programs
First, populate the empty database with the data generator, then
use the other program to run the built in queries.
The Relations file contains the schema definitions.
Server ran locally with MySQL.
Program allows for some large queries, two complex queries,
and the addition and removal of students.
The result can be viewed by asking program to list students.